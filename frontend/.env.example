VITE_API_BASE_URL=http://localhost:3001

# Optional: Enable Twilio STUN in addition to Google STUN (off by default)
# Set to '1' to enable
VITE_USE_TWILIO_STUN=0

# Optional: TURN server (recommended for NAT traversal)
# Example TURN settings (match dokploy turn service):
# VITE_TURN_URL=turn:your.turn.domain:3478
# VITE_TURN_USERNAME=
# VITE_TURN_CREDENTIAL=

# Force using TURN relay only (for diagnostics). Set to '1' to force.
VITE_ICE_RELAY_ONLY=0
